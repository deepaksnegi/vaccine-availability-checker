import { createMuiTheme } from "@material-ui/core";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#92E3A9",
    },
    secondary: {
      main: "#ffffff",
    },
    background: {
      default: "#f4f5fd",
    },
  },
});

export default theme;
